# Demo video

A webpage that can be recorded as our full demo video

Serve with `cd demo && python -m http.server 8000` and open `http://localhost:8000/?autoplay=true` in your browser. Use browser resolution 1280x720 (or close to it), record the tab and click the button to play.